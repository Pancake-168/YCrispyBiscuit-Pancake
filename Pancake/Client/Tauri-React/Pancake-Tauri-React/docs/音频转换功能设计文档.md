# 音频转换功能设计文档

---

## 一、功能概述

"音频转换"（Audio Switch）是 Pancake 工具箱的音频处理模块。核心能力：

- 7 种主流音频格式互转
- 保持原始音质（默认最高码率）
- 展示时长、采样率等音频元数据
- 单文件与批量处理

对比图片转换：没有质量滑块、缩放裁切、色彩模式、透明通道、魔数检测。用户只选目标格式，点转换。

底层走 ffmpeg + ffprobe，二进制随 PyInstaller 打包进 exe。

---

## 二、格式覆盖

### 2.1 格式列表

| 格式 | 扩展名 | 有损 | MIME | 选择理由 |
|---|---|---|---|---|
| WAV | .wav | 否 | audio/wav | 无损无压缩，兼容性最广 |
| FLAC | .flac | 否 | audio/flac | 无损压缩，开源，音乐存档标准 |
| AIFF | .aiff / .aif | 否 | audio/aiff | Mac 平台的 WAV 等价物，专业音频常用 |
| MP3 | .mp3 | 是 | audio/mpeg | 全球兼容性最好 |
| AAC | .m4a / .aac | 是 | audio/mp4 | Apple 生态标准，YouTube 音频流，使用量仅次于 MP3 |
| OGG Vorbis | .ogg | 是 | audio/ogg | 开源有损，同码率音质优于 MP3 |
| OPUS | .opus | 是 | audio/opus | 低码率高质量，语音/流媒体首选，IETF 标准 |

7 种格式全部双向互转。

### 2.2 未列入的格式

| 格式 | 不列入的理由 |
|---|---|
| WMA | 微软私有格式，ffmpeg 解码受限，编码支持不稳定。存量用户极少，不值得为边缘格式增加维护成本 |
| ALAC | Apple 无损。可被 WAV/FLAC/AIFF 三种无损覆盖，转出去用户也能用。多一种无损格式只是增加选项数，不增加实用价值 |
| DSD / SACD | 发烧友领域，文件巨大、硬件绑定，不属于"日常工具箱"范围 |

### 2.3 容器与编码器关系

用户容易混淆 OGG/OPUS 和 AAC/M4A。以下说明放在前端格式选择器旁边：

- `.ogg` = OGG 容器 + Vorbis 编码 → 格式标识 `ogg`
- `.opus` = OGG 容器 + Opus 编码 → 格式标识 `opus`
- `.m4a` = MP4 容器 + AAC 编码 → 格式标识 `aac`
- `.aac` = 裸 AAC 流（无容器） → 极少见，也支持

选择格式时用户选的是**编码方式**（Vorbis 还是 Opus、AAC 用什么容器），扩展名由后端统一处理，前端不暴露这个细节。

### 2.4 格式特有限制

- **有损→无损**：不会丢失更多质量，但也不会恢复已丢失的信息。MP3→FLAC 只是换容器，音质等同于原 MP3
- **无损→有损**：WAV/FLAC/AIFF→有损格式会有首次有损编码，由 ffmpeg 默认质量决定
- **元数据**：ffmpeg 自动保留可映射字段（标题/作者/专辑），不兼容字段静默丢弃
- **WAV 4GB 上限**：标准 RIFF 头不支持 >4GB。ffmpeg 自动使用 RF64 扩展，但极老的播放器可能不兼容。对日常使用无影响

---

## 三、后端设计（FastAPI）

### 3.1 代码结构

对齐 PictureSwitch 的 `services/PictureSwitch/` 子目录模式，命名前缀统一为 `AudioSwitch`：

```
app/
├── utils/AudioSwitchUtils.py
├── schemas/AudioSwitchSchema.py
├── services/AudioSwitch/
│   ├── AudioSwitchService.py
│   └── AudioSwitchTaskStore.py
└── controllers/AudioSwitchController.py
```

路由注册（`app/api/router.py` 新增两行）：

```python
from app.controllers import AudioSwitchController
router.include_router(AudioSwitchController.router, prefix="/api")
```

### 3.2 API 端点

#### 获取格式列表

```
GET /api/audio/formats
```

响应：

```json
{
  "input_formats": ["aac", "aiff", "flac", "mp3", "ogg", "opus", "wav"],
  "output_formats": ["aac", "aiff", "flac", "mp3", "ogg", "opus", "wav"],
  "format_details": {
    "wav":  { "extensions": [".wav"],  "mime_type": "audio/wav",  "lossy": false },
    "flac": { "extensions": [".flac"], "mime_type": "audio/flac", "lossy": false },
    "aiff": { "extensions": [".aiff", ".aif"], "mime_type": "audio/aiff", "lossy": false },
    "mp3":  { "extensions": [".mp3"],  "mime_type": "audio/mpeg", "lossy": true  },
    "aac":  { "extensions": [".m4a", ".aac"], "mime_type": "audio/mp4", "lossy": true  },
    "ogg":  { "extensions": [".ogg"],  "mime_type": "audio/ogg",  "lossy": true  },
    "opus": { "extensions": [".opus"], "mime_type": "audio/opus", "lossy": true  }
  }
}
```

Schema 中的 `FormatDetail` 与图片版同名但不同构，各模块独立定义，不会冲突。音频版只有 3 个字段（extensions / mime_type / lossy），不含 supports_transparency、quality_range、supports_lossless。

#### 批量转换

```
POST /api/audio/convert
Content-Type: multipart/form-data
```

参数：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| files | File[] | 是 | 音频文件，前后端均限制 ≤200MB |
| target_format | string | 是 | 目标格式标识，如 `"mp3"` |

响应：

```json
{
  "task_id": "a1b2c3d4e5f6",
  "total": 3,
  "results": [
    {
      "index": 0,
      "original_name": "song.wav",
      "converted_name": "song.mp3",
      "original_format": "wav",
      "target_format": "mp3",
      "original_size": 52860044,
      "converted_size": 4806128,
      "size_ratio": 0.0909,
      "duration_seconds": 212.5,
      "sample_rate": 44100,
      "status": "success",
      "error": null
    }
  ]
}
```

与图片转换 ConvertResultItem 对比：移除 `original_resolution` / `converted_resolution`，新增 `duration_seconds` / `sample_rate`。

#### 下载

```
GET  /api/audio/download/single/{task_id}/{index}
GET  /api/audio/download/batch/{task_id}
```

与图片转换完全一致。

### 3.3 AudioSwitchService 核心逻辑

对照 PictureSwitchService 逐环节对齐：

```python
import os, shutil, subprocess, asyncio, json
from pathlib import Path
from concurrent.futures import ThreadPoolExecutor

# ---- ffmpeg / ffprobe 路径 ----
_BUNDLED_FFMPEG = BASE_DIR / "ffmpeg/bin/ffmpeg.exe"
_BUNDLED_FFPROBE = BASE_DIR / "ffmpeg/bin/ffprobe.exe"

FFMPEG = str(_BUNDLED_FFMPEG) if _BUNDLED_FFMPEG.exists() else shutil.which("ffmpeg")
FFPROBE = str(_BUNDLED_FFPROBE) if _BUNDLED_FFPROBE.exists() else shutil.which("ffprobe")

if not FFMPEG or not FFPROBE:
    raise ConfigurationError("ffmpeg/ffprobe 未找到，请运行 npm run download:ffmpeg")

_AUDIO_EXECUTOR = ThreadPoolExecutor(max_workers=2, thread_name_prefix="audio-")

MAX_FILE_SIZE = 200 * 1024 * 1024
MAX_FILES = 50


def _probe_metadata(path: str) -> dict:
    """ffprobe 提取音频元数据。失败时返回默认值，让 ffmpeg 继续尝试转换。"""
    try:
        result = subprocess.run(
            [FFPROBE, "-v", "quiet", "-print_format", "json",
             "-show_format", "-show_streams", path],
            capture_output=True, text=True, check=True,
        )
        info = json.loads(result.stdout)
        fmt = info.get("format", {})
        stream = next((s for s in info.get("streams", []) if s["codec_type"] == "audio"), {})
        return {
            "duration": float(fmt.get("duration", 0)),
            "sample_rate": int(stream.get("sample_rate", 0)),
        }
    except (subprocess.CalledProcessError, json.JSONDecodeError,
            ValueError, KeyError, StopIteration):
        logger.warning("ffprobe 读取元数据失败: %s", path)
        return {"duration": 0.0, "sample_rate": 0}


def _convert_one_sync(self, index, filename, data, params, task_dir):
    # 1. 文件大小校验（后端防御）
    if len(data) > MAX_FILE_SIZE:
        raise BadRequestError(
            f"文件过大（>{MAX_FILE_SIZE // (1024 * 1024)}MB）: {filename}"
        )

    # 2. 扩展名白名单检查
    if not is_supported_input(filename):
        raise BadRequestError(f"不支持的音频格式: {filename}")

    # 3. 原始信息
    ext = Path(filename).suffix.lower()
    original_format = ext.lstrip(".")

    # 4. 写入临时输入文件
    input_path = task_dir / f"_in_{index}{ext}"
    input_path.write_bytes(data)

    # 5. ffprobe 读取元数据
    metadata = _probe_metadata(str(input_path))

    # 6. 计算输出路径（含重名处理）
    target_ext = get_target_extension(params.target_format)
    converted_name = change_extension(filename, target_ext)
    output_path = task_dir / converted_name
    counter = 1
    while output_path.exists():
        stem = Path(filename).stem
        converted_name = f"{stem}_{counter}{target_ext}"
        output_path = task_dir / converted_name
        counter += 1

    # 7. ffmpeg 转换
    try:
        subprocess.run(
            [FFMPEG, "-y", "-i", str(input_path), str(output_path)],
            check=True,
            stdout=subprocess.DEVNULL,
            stderr=subprocess.PIPE,
            text=True,
        )
    except subprocess.CalledProcessError as e:
        input_path.unlink(missing_ok=True)
        output_path.unlink(missing_ok=True)
        stderr_tail = e.stderr.strip().split('\n')[-2:] if e.stderr else []
        raise BadRequestError(
            f"音频转换失败: {filename} — {'; '.join(stderr_tail)}"
        ) from e

    # 8. 读取结果并清理临时输入
    converted_data = output_path.read_bytes()
    input_path.unlink(missing_ok=True)

    return {
        "index": index,
        "original_name": filename,
        "converted_name": converted_name,
        "original_format": original_format,
        "target_format": params.target_format,
        "original_size": len(data),
        "converted_size": len(converted_data),
        "size_ratio": round(len(converted_data) / len(data), 4),
        "duration_seconds": round(metadata["duration"], 1),
        "sample_rate": metadata["sample_rate"],
        "status": "success",
        "error": None,
    }
```

关键对齐点：

- `loop.run_in_executor(_AUDIO_EXECUTOR, self._convert_one_sync, ...)` — 包装到线程池
- 文件大小校验 — 后端防御层
- `is_supported_input()` — 扩展名白名单
- `task_dir` — 任务级隔离目录
- 重名处理 — `_1, _2` 后缀去重
- stderr 包含在异常消息中 — 用户能看到具体错误原因
- 异常分支清理临时文件 — 不残留 `_in_*` 和不完整输出

### 3.4 AudioSwitchUtils

```python
from typing import Optional, Dict, Any, List
from pathlib import Path

# 扩展名 → MIME 类型
EXT_TO_MIME: Dict[str, str] = {
    ".wav":  "audio/wav",
    ".flac": "audio/flac",
    ".aiff": "audio/aiff",
    ".aif":  "audio/aiff",
    ".mp3":  "audio/mpeg",
    ".m4a":  "audio/mp4",
    ".aac":  "audio/mp4",
    ".ogg":  "audio/ogg",
    ".opus": "audio/opus",
}

# 格式标识 → 标准扩展名（含点）
FORMAT_TO_EXT: Dict[str, str] = {
    "wav":  ".wav",
    "flac": ".flac",
    "aiff": ".aiff",
    "mp3":  ".mp3",
    "aac":  ".m4a",
    "ogg":  ".ogg",
    "opus": ".opus",
}

SUPPORTED_EXTENSIONS = set(EXT_TO_MIME.keys())


def is_supported_input(filename: str) -> bool:
    return Path(filename).suffix.lower() in SUPPORTED_EXTENSIONS


def get_target_extension(target_format: str) -> str:
    fmt = target_format.lower().lstrip(".")
    return FORMAT_TO_EXT.get(fmt, f".{fmt}")


def change_extension(filename: str, new_ext: str) -> str:
    return f"{Path(filename).stem}{new_ext}"
```

对比 PictureUtils（~500 行），AudioUtils 仅 ~40 行。没有魔数检测、没有缩放计算、没有色彩模式、没有保存参数工厂函数。

### 3.5 AudioSwitchTaskStore

独立但结构相同。`StoredFile` 移除 `converted_resolution`：

```python
@dataclass
class StoredFile:
    path: Path
    converted_name: str
    converted_size: int
```

任务生命周期与 PictureTaskStore 一致：`OUTPUT_DIR / task_id` 隔离、`TASK_CLEANUP_SECONDS` 到期清理、`MAX_TASKS` LRU 淘汰。

---

## 四、前端设计

### 4.1 文件清单

| 文件 | 对应 | 说明 |
|---|---|---|
| `src/ApiUrls.ts` | 已有 | 新增 4 个音频 API URL |
| `src/services/AudioSwitch.ts` | PictureSwitch.ts | API 调用、类型定义、文件过滤 |
| `src/views/Pages/AudioSwitchPage/index.tsx` | PictureSwitchPage | 页面组件（骨架已有） |
| `src/views/Pages/AudioSwitchPage/index.module.css` | index.module.css | CSS Module |

### 4.2 UI 布局

```
┌──────────────────────────────────────────────┐
│  页面标题：音频转码                              │
│  支持 aac / aiff / flac / mp3 / ogg / opus / wav │
│                                              │
│  ┌──────────────────────────────────────┐     │
│  │  🎵  拖拽音频文件到此处，或点击选择      │     │
│  │      已选: 3 个                       │     │
│  └──────────────────────────────────────┘     │
│                                              │
│  目标格式: [▼ mp3           ]                │
│  [      开始转换（3 个）           ] (btn)    │
│                                              │
│  ┌── 转换结果 ─────────────────────────┐    │
│  │ ✅ song.wav → song.mp3              │    │
│  │    50.3 MB → 8.1 MB · 16.1%        │    │
│  │    3分42秒 · 44100Hz               │    │
│  │                          [下载]      │    │
│  │ ❌ broken.aiff                      │    │
│  │    转换失败: Invalid data found...  │    │
│  └─────────────────────────────────────┘    │
└──────────────────────────────────────────────┘
```

### 4.3 拖拽区及文件列表

完全复用 PictureSwitchPage 的模式：

- 拖拽区用原生 `<div>` + onDragOver/onDragLeave/onDrop/onClick
- 隐藏 `<input type="file">`，accept 由 `supportedExtensions.map(e => '.'+e).join(',')` 动态构建
- 文件过滤函数 `filterSupportedFiles()` 在 AudioSwitch.ts 中定义，与 PictureSwitch 同名不同实现
- 单文件上限 200MB，批量上限 50 个

文件列表每行展示（转换前）：

| 信息 | 来源 | 方式 |
|---|---|---|
| 音频图标 | VscFileMedia | 视觉锚点，替代图片缩略图 |
| 文件名 | File.name | 直接展示 |
| 格式 + 大小 | 扩展名 + File.size | 副行文字，用 formatFileSize() |
| 时长 | 暂不展示 | 转换前拿时长需要解码整个文件，成本太高。转换后由后端返回填充 |

### 4.4 结果列表

每行展示（转换后）：

| 信息 | 来源 | 方式 |
|---|---|---|
| 状态图标 | VscCheck / VscWarning | 成功绿色 ✓ / 失败橙色 ⚠ |
| 文件名转换前后 | 后端返回 | `original_name → converted_name` |
| 大小变化 | 后端返回 | `50.3 MB → 8.1 MB · 16.1%` |
| 时长 | 后端返回 duration_seconds | `3分42秒`（前端格式化） |
| 采样率 | 后端返回 sample_rate | `44100Hz` |
| 错误信息 | 后端返回 error | 失败时显示具体原因 |
| 下载按钮 | taskId + index | 单文件下载 |

时长格式化函数（新增到 `src/utils/file.ts`）：

```typescript
export function formatDuration(seconds: number): string {
  if (seconds <= 0) return '0秒';
  if (seconds < 60) return `${Math.floor(seconds)}秒`;
  if (seconds < 3600) {
    const m = Math.floor(seconds / 60);
    const s = Math.floor(seconds % 60);
    return `${m}分${s}秒`;
  }
  const h = Math.floor(seconds / 3600);
  const m = Math.floor((seconds % 3600) / 60);
  return `${h}小时${m}分`;
}
```

### 4.5 组件使用清单

| UI 元素 | 组件 | 备注 |
|---|---|---|
| 格式选择器 | `<Select>` | 选项来自 getFormats() 动态获取 |
| 开始转换按钮 | `<Button variant="primary" block loading>` | 文件为空时 disabled，转换中显示 loading |
| 文件列表容器 | `<ScrollArea maxHeight={280}>` | 与图片转换一致 |
| 拖拽区空状态 | `<EmptyState>` | 无文件时显示 |
| 加载骨架 | `<Skeleton>` | 格式列表加载中 / 转换等待中 |
| 操作反馈 | `toast()` | 转换完成/失败通知 |
| 拖拽区 | 原生 `<div>` + 事件 | DRY，项目无 DragZone 组件 |
| 文件选择input | 原生 `<input type="file">` | 浏览器无替代方案 |
| 文件图标 | VscFileMedia | react-icons/vsc 内置 |
| 状态图标 | VscCheck / VscWarning | 已有使用 |

不需要引入新依赖。所有组件和图标已在项目中。

### 4.6 状态管理

只用一个 ConvertStatus：`'idle' | 'converting' | 'done'`，与图片转换完全一致。

状态机：
```
idle → (点转换) → converting → (全部完成) → done → (再选文件) → idle
done → (直接再点转换) → converting
```

---

## 五、构建与打包

### 5.1 需要打包的二进制

ffmpeg 发行版包含 3 个 exe：

| 文件 | 大小 | 用途 | 是否打包 |
|---|---|---|---|
| ffmpeg.exe | ~97MB | 音频编码/转码 | ✅ 打包 |
| ffprobe.exe | ~97MB | 读取元数据（时长/采样率） | ✅ 打包 |
| ffplay.exe | ~99MB | 音频播放（命令行） | ❌ 不打包 |

上次说要只打包 ffmpeg.exe，但深入分析后 ffprobe 是用来获取时长的，必须打包。两文件合计 ~194MB。

打包命令：

```
--add-data "ffmpeg/bin/ffmpeg.exe;ffmpeg/bin"
--add-data "ffmpeg/bin/ffprobe.exe;ffmpeg/bin"
```

.spec datas 同理追加两条。

### 5.2 ffmpeg 可用性检查

服务启动时检查两文件存在性，任一缺失则抛 `ConfigurationError`。理由是 `npm run download:ffmpeg` 下载的是完整包，两个文件天然存在——如果其中一个不存在说明下载不完整或用户手动删了，应该明确报错。

---

## 六、与图片转换差异总览

| 维度 | 图片转换 | 音频转换 |
|---|---|---|
| 参数数量 | ~14 个 Form 字段 | 1 个（target_format） |
| 格式数 | 12 输入 / 11 输出 | 7 输入 / 7 输出 |
| 后端行数 | ~1500 行 | ~300 行 |
| 前端行数 | ~850 行 | ~400 行 |
| 前端控件 | 10+ 个 | 3-4 个 |
| 子目录 | `services/PictureSwitch/` | `services/AudioSwitch/` |
| 元数据展示 | 分辨率（width×height） | 时长 + 采样率 |
| 缩略图/图标 | blob URL 缩略图 | VscFileMedia 图标 |
| 魔数检测 | 需要 | 不需要 |
| 底层依赖 | Pillow + pillow-heif + numpy + cairosvg | ffmpeg + ffprobe（独立 exe） |
| 包体积增加 | ~50MB | ~194MB |
