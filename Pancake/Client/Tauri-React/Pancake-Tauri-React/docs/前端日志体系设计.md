ts项目比如react、vue等均可参考此规则：

前端console这个日志功能是能够在web的控制台里面输出的，但前端生产桌面端后，这份console需要被保留下来成为真实日志记录的样子。

统一函数体系：logger

前端在任意一处需要输出日志时，只需要调用函数 logger.info/logger.warn/logger.error等即可

函数内部自动判定当前环境，web就直接默认console.log/warn/error
检测到桌面端时，将其写入真实日志文件内。
