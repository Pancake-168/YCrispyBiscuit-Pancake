记录一下项目创建过程：
1. npm create vite@latest
2. 确认lint风格检查是否有效，没有就安装
3. 确认format格式检查是否有效，没有就安装 
   npm install --save-dev prettier eslint-config-prettier
   eslint-plugin-prettier
4. 兼容'@/xxx/xxx'的引入方式
5. 两个.env文件的编写
6. 桌面端是否具备? tauri? electron?
7. 引入存储、路由

N. 文件夹架构：
-- components
-- composables
-- constants
-- language?（i18n）
-- router
-- services 
-- stores
-- styles
-- types
-- utils 
-- views
-- apiurls.ts

