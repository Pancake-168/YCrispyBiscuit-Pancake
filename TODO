给前端构建一套完整的日志系统：

范围涵盖：
1.electron桌面端的日志能力
2.web端的F12控制台console能力

输出风格：
console.log("[Pancake:FileName:FunctionName]InfoMessage");
也就是在日志输出中包含文件名和函数名，以便更好地定位问题。

桌面端的话，已经将先前不完整的日志系统删掉了，接下来的完整日志系统，应该也不会写进startlog里面了，专业的桌面端软件应该有一套独立的日志系统，来记录各种日志信息，包括错误日志、调试日志、操作日志等。
印象中应该都位于log文件夹内部。