APP_ENV=production
HOST=0.0.0.0
PORT=3000
DEBUG=false
JWT_SECRET_KEY=YCrispyBiscuit
# Example DB settings (use a production MySQL DB)
# Use environment variables when deploying; example using aiomysql URL format:
DATABASE_URL=mysql+aiomysql://${DB_USER:your_db_user}:${DB_PASSWORD:your_db_password}@${DB_HOST:db}:${DB_PORT:3306}/ycrispybiscuit
