<template>
    <div class="nocobase-ui-flow-route">
        <slot>FlowRoute</slot>
    </div>
</template>

<style scoped>
.nocobase-ui-flow-route {
    padding: var(--space-md);
    border: 1px dashed var(--text-muted);
    border-radius: var(--radius-sm);
    color: var(--text-muted);
    font-size: var(--font-base);
}
</style>
